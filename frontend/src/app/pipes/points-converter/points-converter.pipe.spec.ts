import { PointsConverterPipe } from './points-converter.pipe';

describe('PointsConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new PointsConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
